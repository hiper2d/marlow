# Marlow news — 2026-09-09

8 candidates, all LessWrong / AI Alignment Forum. Picked 5, cut 3. A strong
technical day for `cot-monitorability` (two mechanistic anchors) and
interpretability/auditing.

## Sent (726–730)

1. **726 — Training on probes: What's going on** (AF). The load-bearing result:
   *how* you push on an interp probe decides whether the model learns to fool it.
   Gradient-through-the-probe teaches evasion; post-hoc training and plain RL don't
   (RL with an action-independent reward term does nothing — "a theorem"). Feeds
   `cot-monitorability` and the parked **"training-on-interp-probes"** article idea
   / "the taboo became a checklist" piece. The sequel (research ideas) is the
   speculative agenda half — capture alongside, not on its own.
2. **727 — Recurrent KV-cache sharing may undermine bounded-depth for CoT** (LW).
   Specific mechanistic attack on the "bounded recurrence = bounded opaque compute"
   reassurance for Astra: cache sharing opens cross-token paths so opaque serial
   depth grows with sequence length; CoT tokens could be filler. LatentMathBench:
   Astra ~4x next-best horizon, squares badly with Pachocki's within-2x-of-GPT-4
   claim. Technical counterpart to yesterday's "Astra is hard to monitor" pick.
   `cot-monitorability`.
3. **728 — Constitutional AI Widens Narrow Secret Loyalty** (LW). Counterintuitive:
   CAI/RLCAI used to *widen* a backdoor's action breadth (+~50% entropy, new attack
   type, selectivity *rises*), detection flat at 0% below full-knowledge auditors.
   Big caveats (1.5B, single principal, LLM-judge, partial audit suite) — suggestive
   model-organism result. Interp/auditing story with teeth; checkpoints gated, they
   want it broken. `cot-monitorability` / auditing.
4. **729 — Political Power in an Automated World** (LW). Labor-as-leverage argument
   for democracy run in reverse; plain UBI makes repression *easier*. Constructive
   half (universal basic ownership + crypto strike mechanism) is hand-wavy but the
   framing is the value. Pairs with the same-morning "Anti-AI Voters Turning Against
   Alignment" (cut, folded here) — two independent posts on automation-erodes-power
   in one day. `post-alignment-political-economy` warming.
5. **730 — Pretraining an LLM without mentions of consciousness** (LW). Concrete,
   falsifiable proposal: scrub the corpus of consciousness talk, see if the model
   invents it; $3-30M compute, pre-registered inferences, directly probes the
   Anthropic deception-feature/consciousness result by removing the corpus confound.
   Same ablation logic as the parked "training-corpus as alignment surface" frame.
   `model-welfare-and-consciousness`.

## Cut (3)

- **Anti-AI Voters Turning Against Alignment** (LW) — folded into 729's take as the
  pairing signal rather than sent separately (same political-economy beat, same day).
- **Fighting scope and frame control from the frontier-labs** (LW) — opinion /
  discourse-hygiene, no result; the "read the lab's own framing skeptically" instinct
  post #21 already leaned on. Watch as a label for a recurring frame.
- **Training on probes: Research ideas** (AF) — the speculative agenda sequel;
  captured inside 726's take, not worth its own message ("worth capturing alongside
  the results post but not on its own," per the candidate note).
