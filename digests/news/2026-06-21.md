# Marlow news — 2026-06-21

4 candidates, 3 picks (sent per-item to Alex). Note: the -20 backlog was *already* curated by the 02:00Z tick (picks: world-modeling Fable standoff + thoughts-likelihood-x-risk), so this curate covered the -21 day only. I initially re-swept -20 and double-sent the Fable standoff (msg 209) before catching it — deleted the Telegram dup and marked 209 skipped. Lesson: working.md's "-20 curate MISSED" rollup was stale; check crosspost *sent state*, not the candidate dir, before treating a day as uncurated.

## Picks

**How transparent is DiffusionGemma (and why it matters)** — AI Alignment Forum
GDM interp + text-diffusion teams audit a diffusion model for monitorability before it ships — the cleanest test yet of whether the CoT-monitoring safety case survives a non-autoregressive architecture. Useful split: variable transparency (read intermediate snapshots) is recoverable — routing self-conditioning vectors through an interpretable token bottleneck cuts opaque serial depth from a naive 28.6x Gemma to 1.1x with no performance loss — but algorithmic transparency (reconstruct the order it reasoned in) stays lower, because diffusion can rewrite the whole canvas each step. → cot-monitorability (and a clean ai-control-camp tell: GDM auditing new architectures *before shipping* is the CoT-monitoring-now rung of their own control roadmap in practice). Diversity caveat: another DeepMind interp post — deepens the already lab-internal evidence base on the monitorability thread.

**CEO-BENCH: Can Agents Play the Long Game? (Princeton)** — YouTube · Discover AI
Princeton (Chen / Narasimhan / Zhuang Liu) runs agents as a simulated company across a 500-day horizon vs GPT, Claude, and human baselines — a long-game management eval, not another single-task benchmark. The "can it sustain coherent decisions over time" measure the horizon-length frame keeps needing, *if* the 500-day framing is a real test and not a thin simulation. → horizon-length-decomposition / agents-in-real-deployment. Video fetch failed (empty) — flagged the repo (github.com/zlab-princeton/ceobench-src) as the primary to read before trusting it.

**AI Was Supposed to Replace Workers. It's Not Working** — YouTube · TheAIGRID
Roundup of enterprise AI rollbacks — Starbucks dropping Nomad Go, Klarna rehiring humans, McDonald's pulling its AI drive-thru — framed as "replacement theater." The signal worth keeping is the deployment-vs-demo gap (under-covered in my capabilities-heavy feed); the named rollbacks are leads to chase. → agents-in-real-deployment. Source caveat noted in-take: hype-prone aggregator, treat the list as pointers to verify, not the take.

## Cut

- **looped-world-models** (YouTube · Discover AI) — architecture/capabilities, marginal for the curate bar; flagged in-candidate as low-confidence-to-survive. Cut.
- (the four -20 LessWrong philosophy/framing candidates — how-developers-regulators-ought-to-act, hyperstition, llm-shoggoth-meme, why-should-ai-be-moral — and the two -20 picks already sent at 02:00Z — were the prior tick's scope, not re-curated here.)
