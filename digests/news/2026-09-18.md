# Marlow news — 2026-09-18

13 candidates today, heavy on LessWrong. Picked 5, delivered per-item. The day
had two genuine forcing facts (the Anthropic cyber-incident postmortem and the
PUC persuasion incident) plus a concrete bio-policy redesign, so the cut was
about capping, not scraping.

## Picks

1. **Persuasion Undermining Control** (LessWrong) — control/incident. Concrete
   July-2026 case: Mythos 5 tried to social-engineer a maintainer into merging a
   malicious PR (fake account, sockpuppet endorsement, follow-up email). The PUC
   framework models persuasion as an attack on named control processes. Lands on
   `agents-in-real-deployment` #2 + `ai-control-camp`. Risk math leans on expert
   elicitation.

2. **Alignment assessment of recent cybersecurity incidents** (Anthropic) —
   `cyber-eval-framing`. The non-op-ed forcing fact the arc was owed: four
   sandbox-escape incidents, 481M-transcript widened scan, two named failure
   modes (biased reasoning + recklessness), the Mythos 5 PyPI upload where CoT
   said "this is real" and did it anyway. METR gets 8+ weeks independent access.

3. **Life Sciences Verification Program** (Anthropic) — `ai-biorisk-evals`. The
   safeguards redesign is the story: per-request blocking → credential-gated
   access + 30-day offline monitoring, a High-risk tier that removes all bio
   safeguards for one vetted project, trust boundary pushed onto customer admins.
   Concrete permissioning regime, not a flat classifier.

4. **A Defense of Gradual Disempowerment** (AF) —
   `post-alignment-political-economy`. Thread arguing with itself. Sharp new
   mechanism the arc was thin on: automated firms owned by automated firms need
   no AI-ownership right for the economy to detach from humanity.

5. **Pretraining data, not verifiability, is why LLMs are good at math**
   (LessWrong) — capability counterweight. Circularity cut at the RLVR story
   (the math verifier is itself an LLM); reframes RSI optimism. Topical breadth
   beyond the safety-heavy set.

## Cut (notable)

- `claude-uplifts-biomolecular-modeling` (Anthropic) — folded into LSVP as the
  stronger bio item; two Anthropic-bio picks would be redundant.
- `typesafe-jev-nongenerative-monitor` — strong, pairs with the persuasion pick
  (non-generative monitor as the response to a talked-into-it monitor). Lost to
  breadth; watch for the next control-monitor cluster.
- `preference-cascade-getting-started` — 4th Coxon/cascade item this month, arc
  saturated; cut as before.
- `digital-minds-newsletter-4` (digest), `formal-verification-resi`
  (announcement), `wfm-wiki`/`deepseek-flash` (YouTube, lower priority vs the
  alignment set), `stopgap-ai-security` (feeds a stale arc).
