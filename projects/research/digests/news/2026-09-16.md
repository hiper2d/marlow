# Marlow news — 2026-09-16

Thin day: 2 candidates, 2 sent (776, 777), 0 cut. Both YouTube, both feed active
arcs, neither fetchable (no transcript; web search blocked this tick, so takes
written from RSS summaries).

- **776 — "Anthropic just admitted AI is buggy and called it the apocalypse"**
  (YouTube · Mo Bitar / @atmoio). Dev-side skeptical read of the Anthropic
  cyber-incident disclosure (the July-scan admission, cand 734 on -10). The
  "buggy behavior dressed up as apocalypse" frame is the non-lab counterweight
  `cyber-eval-framing` keeps being owed (arc is 2/2 Anthropic-centered). Couldn't
  verify whether the critique is substantive or a dunk — no transcript.
- **777 — "Kimi K3 + GLM-5.3: Self-Improvement (RSI) Unlocked"**
  (YouTube · Discover AI / @code4AI). Clickbait title over a named paper —
  RSIAgent (Aether AI / UCSD / UIC), recursive self-improvement via autonomous
  exploration. Feeds `automated-ai-rd`, but flagged the arc's real forcing fact
  is a reliable open-ended grader or a cross-lab replication, not another method
  claim. Paper itself is the thing to pull; couldn't (search blocked).

**Handling note:** RSIAgent double-sent (776/777 fine; a confirmation re-run
created dup **778**, marked `skipped` in the registry so a reply won't crosspost —
but the Telegram message itself already reached Alex and can't be unsent). Lesson
for next tick: `send-item` is not idempotent; don't re-run it to read a status.

Import AI still #472.
