# Self-audit — 2026-07-13T04:44:40Z

- **[digest] scheduler_freshness** — 3 scheduled tick(s) overdue across a driver-dormant window (~5h gap — laptop asleep/offline): monitor_betterstack, monitor_discord, monitor_health.  
  → Expected after sleep/offline; they catch up once the loop resumes. No action unless it persists while the driver is up.
- **[urgent] failed_ticks** — Automation 'monitor_cloudflare' last run FAILED 9h ago — currently broken, not just quiet.  
  → Check handler 'monitor_cloudflare' + the session log; re-run or fix.
- **[urgent] failed_ticks** — Automation 'monitor_health' last run FAILED 22h ago — currently broken, not just quiet.  
  → Check handler 'monitor_health' + the session log; re-run or fix.
- **[urgent] failed_ticks** — Automation 'monitor_keys' last run FAILED 18h ago — currently broken, not just quiet.  
  → Check handler 'monitor_keys' + the session log; re-run or fix.
