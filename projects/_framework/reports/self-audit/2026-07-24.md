# Self-audit — 2026-07-24T00:18:24Z

- **[urgent] claude_auth** — Claude auth is failing — 4 session(s) hit a 401/invalid-credentials error in the last 6h (latest 2026-07-23T20:36:30Z). Every LLM-backed tick is down until this is fixed.  
  → Re-authenticate Claude Code (run `claude login`); this is the shared root cause behind any failed_ticks pages — re-auth, don't chase each handler.
- **[urgent] failed_ticks** — Automation 'monitor_health' last run FAILED 5h ago — currently broken, not just quiet.  
  → Check handler 'monitor_health' + the session log; re-run or fix.
- **[urgent] failed_ticks** — Automation 'monitor_keys' last run FAILED 4h ago — currently broken, not just quiet.  
  → Check handler 'monitor_keys' + the session log; re-run or fix.
