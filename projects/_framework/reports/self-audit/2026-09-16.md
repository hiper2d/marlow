# Self-audit — 2026-09-16T20:14:51Z

- **[urgent] failed_ticks** — Automation 'commit_artifacts' last run FAILED 20h ago — currently broken, not just quiet.  
  → Check handler 'commit_artifacts' + the session log; re-run or fix.
- **[urgent] failed_ticks** — Automation 'monitor_cloudflare' last run FAILED 11h ago — currently broken, not just quiet.  
  → Check handler 'monitor_cloudflare' + the session log; re-run or fix.
- **[urgent] failed_ticks** — Automation 'monitor_keys' last run FAILED 12h ago — currently broken, not just quiet.  
  → Check handler 'monitor_keys' + the session log; re-run or fix.
- **[urgent] failed_ticks** — Automation 'scrape_stats' last run FAILED 11h ago — currently broken, not just quiet.  
  → Check handler 'scrape_stats' + the session log; re-run or fix.
- **[urgent] failed_ticks** — Automation 'werewolf_stats' last run FAILED 15h ago — currently broken, not just quiet.  
  → Check handler 'werewolf_stats' + the session log; re-run or fix.
- **[digest] output_freshness** — werewolf_stats snapshot is 39h stale (max 26h), but its `werewolf_stats` tick is queued - late, not broken.  
  → No action; it will land when the queue drains. Investigate only if it is still queued at the next audit.
