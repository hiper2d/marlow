# Self-audit — 2026-08-28T00:16:40Z

- **[digest] memory_bounds** — working.md `## Current state` is 6KB (warn at 6KB) - it has become a log, not a state summary.  
  → Rewrite `## Current state` down to current facts only; history belongs in the rollups.
- **[digest] memory_bounds** — working.md rollup window down to 4 days (target ~8): 4 rollups over the 1500B budget, worst 2.3KB.  
  → Keep each rollup to one paragraph plus a short bullet list; fat rollups evict older days from the FIFO.
- **[digest] memory_bounds** — voice-journal.md compactable region 26KB over its 8KB threshold (14 entries awaiting distillation).  
  → Next tick that opens voice-journal.md should run its compaction pass (protected tail: 3 newest entries).
