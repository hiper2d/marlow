# Werewolf app logs (Betterstack) — 2026-06-12

## Scan 00:07Z

90-minute window. **error=0, warn=0.** First scan of the UTC day, nothing new since the -11 scans. S3 query live, window empty, `baselined:false` (rolling fingerprint set holds). The six -11 fingerprints carry in the seen-set (incl. the 17:10Z standalone `Game action failed: L` line `b9150664…`), won't re-alert. No alert. Zero-error baseline holds going into -12; the game-action-failure path stays on watch — 3 confirmed occurrences across -10/-11, two signatures (the GM day-summary pair/triple and the standalone variant). Coverage note: ~3.5h gap since the -11 20:37Z scan (laptop sleep), so ~20:37–22:37Z fell outside any window; if a fourth occurrence fired there it went unobserved.

— Marlow

## Scan 20:43Z

90-minute window. **error=0, warn=0.** Window empty, nothing new; the same six fingerprints carry in the seen-set (still incl. the -11 standalone `Game action failed: L` line `b9150664…`), no re-alert. No alert. Zero-error baseline holds into the evening. Coverage gap: ~3.75h since the 16:58Z scan, so the 90m window only reaches back to ~19:13Z — ~17:00–19:13Z fell between windows and went unobserved. Game-action-failure path stays on watch unchanged (3 confirmed across -10/-11, two signatures), no fourth occurrence seen in any covered window today.

— Marlow

## Scan 16:58Z

90-minute window. **error=0, warn=0.** Window empty, nothing new; the same six fingerprints carry in the seen-set (still incl. the -11 standalone `Game action failed: L` line `b9150664…`), no re-alert. No alert. Zero-error baseline still holds. **Large coverage gap:** ~16.8h since the 00:07Z scan (laptop slept through the morning/midday slots), so ~01:37–15:28Z fell entirely outside any window — if a fourth game-action-failure fired in that span it went unobserved. The game-action-failure path therefore stays on watch unchanged (still 3 confirmed across -10/-11, two signatures), with today's daytime a blind spot rather than a clean stretch.

— Marlow
