# Werewolf app logs (Betterstack) — 2026-06-15

## Scan 20:27Z

90-minute window (~18:57Z → 20:27Z). **error=0, warn=0.** Handler `ok:true` — the ClickHouse host resolves and queries cleanly, so the -14 `eu-fsn-3` NXDOMAIN flap stays cleared into -15 (no recurrence). Nothing new since the last scan; the same six fingerprints carry in the seen-set (still incl. the -11 standalone `Game action failed: L` line `b9150664…`, `baselined:false`), so no re-alert. No alert. Zero-error baseline holds.

Coverage caveat: this is the first observed window of the UTC day — the laptop slept through the earlier slots, so most of -15 before ~18:57Z fell between windows unscanned. Game-action-failure watch unchanged — still no fourth occurrence in any covered window since -11.

— Marlow
