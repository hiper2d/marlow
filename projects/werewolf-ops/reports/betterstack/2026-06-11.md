# Werewolf app logs (Betterstack) — 2026-06-11

## Scan 00:56Z

90-minute window. **error=0, warn=0.** First scan of the UTC day, nothing new since the -10 scans. S3 query live, window empty, `baselined:false` (rolling fingerprint set holds). The two -10 fingerprints (`e6c27d29…`, `a90ea997…` — the 09:47Z summarizer/game-action pair) still in the seen-set, won't re-alert. No alert. Zero-error baseline holds; the -10 break reads as the confirmed one-off.

## Scan 03:41Z

90-minute window. **error=2, warn=1 — all new.** Three lines from one game at ~02:24Z (02:24:23–02:24:50, ~27s span):

- `[warn] 📝 Failed to generate GM day summary, skipping` (02:24:23.972Z)
- `[error] Error summarizing current day:` (02:24:50.820Z)
- `[error] Game action failed: K` (02:24:50.890Z)

Same summarizer-failure signature as the -10 09:47Z pair (`Error summarizing current day:` → `Game action failed: K`) — GM day-summary generation fails, the summarize step errors, the dependent game action then fails. App-level (the request may die before `errorState` writes, so monitor_health can miss it), recoverable-looking, provider/parse-class — not infra/auth/budget. **This is the second occurrence of the identical signature in ~17h, so the -10 "confirmed one-off" read no longer holds** — the GM day-summary path is the recurring failure mode to watch. The added `warn` this time (the explicit "skipping" before the error pair) suggests the summarizer hit its retry/skip fallback. Per the betterstack alert model (new error → urgent) sent one consolidated urgent. All three fingerprints now in the rolling seen-set; won't re-alert.

## Scan 05:24Z

90-minute window. **error=0, warn=0.** Nothing new since the 03:41Z summarizer pair — that game's lines (02:24Z) are now outside the 90-min window and won't re-alert. Seen-set rolled to five fresh fingerprints (incl. the -10 09:47Z pair `e6c27d29…`/`a90ea997…`). No alert. One clean scan after the 03:41Z occurrence; the GM day-summary failure path stays on watch as the recurring signature (2 occurrences in ~17h across -10/-11).

## Scan 07:06Z

90-minute window. **error=0, warn=0.** Nothing new since the 03:41Z summarizer pair. Seen-set unchanged (same five fingerprints). No alert. Second consecutive clean scan; no third occurrence of the GM day-summary signature inside this window. The recurring summarizer-failure path stays on watch (still 2 confirmed occurrences across -10/-11) but the morning has settled back to the zero-error baseline.

## Scan 13:08Z

90-minute window. **error=0, warn=0.** Note the ~6h gap since the 07:06Z scan (hourly cadence missed — likely laptop sleep), so 07:06–11:38Z was never inside any scan window; if the summarizer signature fired in that span, it went unobserved. What we can say: third consecutive clean scan, seen-set unchanged (same five fingerprints), no alert. GM day-summary failure path stays on watch at 2 confirmed occurrences (-10 09:47Z, -11 02:24Z).

## Scan 17:10Z

90-minute window. **error=1, warn=0 — new.** One line:

- `[error] Game action failed: L` (15:42:46.381Z)

Third occurrence of the game-action-failure class in ~30h (-10 09:47Z pair, -11 02:24Z triple, now -11 15:42Z) — but the signature differs this time: **standalone**, no `Error summarizing current day:` and no GM-day-summary warn preceding it, even though the 90-min window (15:40Z+) would have caught companion lines from the usual ~27s span. So either a different failure path into the same `Game action failed` terminal, or the summarizer succeeded and the action failed on its own. Suffix runs K → L across occurrences — reads like a game-name initial, i.e. different games each time, not one stuck game. Coverage note: cadence gap again (13:08Z scan → this one at 17:10Z), so ~13:08–15:40Z was never inside a scan window. Urgent sent per the alert model (new error line). Fingerprint in the rolling seen-set; won't re-alert.

## Scan 20:37Z

90-minute window. **error=0, warn=0.** Nothing new since the 17:10Z `Game action failed: L` line — that line (15:42Z) is now well outside the window. Seen-set rolled to six fresh fingerprints. No alert. One clean scan after the 17:10Z occurrence; the game-action-failure path stays on watch (3 confirmed occurrences across -10/-11, two distinct signatures: the GM day-summary pair/triple and the standalone variant). Coverage note: another cadence gap (17:10Z → 20:37Z, ~3.5h), so ~17:10–19:07Z fell outside any scan window — if a fourth occurrence fired there it went unobserved.

— Marlow
