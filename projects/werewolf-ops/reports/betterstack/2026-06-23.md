# Betterstack app-log monitor — 2026-06-23

## Scan 00:45:58Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 8 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves and queries, no NXDOMAIN flap. App quiet, baseline holds (the -22 18:31 `Game action failed: I` pair carried into the seen-set, not recurring).

## Scan 01:47:56Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 8 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves and queries, no NXDOMAIN flap. App quiet, baseline holds (the -22 18:31 `Game action failed: I` pair still in the seen-set, not recurring).

## Scan 15:09:24Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 8 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves and queries, no NXDOMAIN flap. App quiet, baseline holds (the -22 18:31 `Game action failed: I` pair still in the seen-set, not recurring).

## Scan 16:31:40Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 8 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves and queries, no NXDOMAIN flap. App quiet, baseline holds (the -22 18:31 `Game action failed: I` pair still in the seen-set, not recurring).

## Scan 17:33:21Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 8 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves and queries, no NXDOMAIN flap. App quiet, baseline holds (the -22 18:31 `Game action failed: I` pair still in the seen-set, not recurring).

## Scan 18:34:58Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 8 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves and queries, no NXDOMAIN flap. App quiet, baseline holds (the -22 18:31 `Game action failed: I` pair still in the seen-set, not recurring).

## Scan 19:36:58Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 8 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves and queries, no NXDOMAIN flap. App quiet, baseline holds (the -22 18:31 `Game action failed: I` pair still in the seen-set, not recurring).

## Scan 20:38:26Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 8 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves and queries, no NXDOMAIN flap. App quiet, baseline holds (the -22 18:31 `Game action failed: I` pair still in the seen-set, not recurring).

## Scan 21:41:13Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 8 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves and queries, no NXDOMAIN flap. App quiet, baseline holds (the -22 18:31 `Game action failed: I` pair still in the seen-set, not recurring).

## Scan 22:22:29Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 8 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves and queries, no NXDOMAIN flap. App quiet, baseline holds (the -22 18:31 `Game action failed: I` pair still in the seen-set, not recurring).

## Scan 23:24:01Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 8 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves and queries, no NXDOMAIN flap. App quiet, baseline holds (the -22 18:31 `Game action failed: I` pair still in the seen-set, not recurring).

## Issues this run

- None new this scan — all green.

— Marlow
