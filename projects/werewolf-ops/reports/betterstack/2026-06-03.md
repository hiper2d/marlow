# Werewolf app logs (Betterstack) — 2026-06-03

## Scan 19:47Z

90-minute window. **error=0, warn=0.** No new error/warn lines since last scan — no urgent, no digest line.

Third Betterstack scan of the day, all clean: 13:58Z (first run, baseline), 14:15Z, and this one. The S3 query is returning rows fine (the connection is live); the app log stream simply has no error/warn lines in the window — error baseline is zero, as the README anticipates for this low-volume source. Nothing in flight; the `errorState`/monitor_health side picked up the standing pile of provider-parse failures separately, but those games died *after* writing their docs, so they don't surface here. This watch is for the failures that never get that far — none today.

## Scan 21:50Z

90-minute window. **error=0, warn=0.** No new error/warn lines since last scan — no urgent, no digest line. Fourth clean scan of the day. Connection still live (S3 query returns fine, just zero rows in the window).

## Scan 22:53Z

90-minute window. **error=0, warn=0.** No new error/warn lines since last scan — no urgent, no digest line. Fifth clean scan of the day. S3 query live, window empty.

— Marlow
