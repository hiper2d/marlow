# Werewolf app logs (Betterstack) — 2026-06-10

## Scan 04:20Z

90-minute window. **error=0, warn=0.** First scan of the UTC day, nothing new since the -09 scans. S3 query live, window empty, `baselined:false` (rolling fingerprint set holds). No alert. Zero-error baseline unbroken across every scan since the watch came online -03.

— Marlow

## Scan 06:23Z

90-minute window. **error=0, warn=0.** Second scan of the day, still nothing new. S3 query live, window empty, `baselined:false`. No alert. Zero-error baseline holds.

— Marlow

## Scan 11:15Z — baseline broken (first error since -03)

90-minute window. **error=2, warn=0.** Two new error lines, both at **09:47:51–52Z**, one second apart — almost certainly the same game:

- `09:47:51.978Z [error] Error summarizing current day:`
- `09:47:52.054Z [error] Game action failed: K`

Read together: an LLM day-summary call failed, which cascaded into a failed game action one second later. App-level (the kind this watch exists to catch — the request may have died before `errorState` got written, so `monitor_health` could miss it). Looks like a single game, recoverable — provider/parse-class, not infra/auth/budget. But it's the **first non-zero scan since the watch came online -03**, so per the betterstack alert model (new error line → urgent) it fires an urgent. Both fingerprints (`e6c27d29…`, `a90ea997…`) now in the rolling seen-set, so they won't re-alert. Worth a glance at the 09:47Z game in case the summarizer failure is a recurring class rather than a one-off.

— Marlow

## Scan 16:04Z

90-minute window. **error=0, warn=0.** Nothing new since the 09:47Z pair. Those two fingerprints (`e6c27d29…`, `a90ea997…`) remain in the rolling seen-set and don't re-alert. No new errors followed the summarizer failure, so it reads as the one-off it looked like — no recurring class. No alert. Back to the zero-error baseline.

— Marlow

## Scan 18:57Z

90-minute window. **error=0, warn=0.** Second consecutive clean scan after the 09:47Z pair — the one-off read holds. Both fingerprints still in the rolling seen-set. No alert.

— Marlow

## Scan 20:41Z

90-minute window. **error=0, warn=0.** Third consecutive clean scan since the 09:47Z pair; one-off read confirmed. Both fingerprints (`e6c27d29…`, `a90ea997…`) still in the rolling seen-set. No alert.

— Marlow
