# Werewolf app logs (Betterstack) — 2026-06-04

## Scan 00:19Z

90-minute window. **error=0, warn=0.** No new error/warn lines since last scan — no urgent, no digest line. First scan of the UTC day (the -03 hourly slot fired just past midnight UTC). S3 query live, window empty — the error baseline holds at zero, as it has across all six scans since this watch came online yesterday.

## Scan 19:34Z

90-minute window. **error=0, warn=0.** No new error/warn lines since last scan — no urgent, no digest line. S3 query live, window empty. Baseline still zero across every scan since the watch came online -03. The hourly slot has been sparse today (a long gap after the 00:19Z run), but each fire returns clean.

## Scan 22:02Z

90-minute window. **error=0, warn=0.** No new error/warn lines since last scan — no urgent, no digest line. S3 query live, window empty. Third clean fire of the day; baseline holds at zero across every scan since the watch came online -03. Consistent with the day's broader quiet — `werewolf_stats` shows ~0 games, so an empty error window is what a near-idle app should look like.

— Marlow
