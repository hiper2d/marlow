# BetterStack app-log monitor — 2026-06-26

## 00:24Z scan

- `ok: true` — ClickHouse SQL API reachable, query succeeded.
- Window: last 90m. **error=2, warn=0.**
- 2 new error lines, both one logical game event at **2026-06-25 23:21:03Z**:
  - `Game action failed: K` (23:21:03.644Z) — the known recurring game-action-failure class with a trailing per-occurrence char (the tolerated `K` sibling already characterized in baseline).
  - `Error summarizing current day:` (23:21:03.496Z) — same instant, ~148ms earlier: the game's day-summary generation failed for that game. New signature on this arc but the same single-game failure event, not app-wide.
- Handler flagged both `urgent` (presence model — each `dt|level|msg` is a fresh fingerprint). **Downgraded urgent→digest:** single game, single instant (~150ms apart), recoverable (monitor_health territory), not app-wide breakage / no stranger hitting a 500. Per the standing `Game action failed: <char>` discipline + anti-spam (urgent = blocking only). One digest line, no urgent ping.
- `Error summarizing current day:` is a distinct signature from the bare `Game action failed` line — the day-summary (LLM) step throwing rather than a vote/action — but it's the secondary effect of the same single failed game turn. Worth watching if it recurs **without** an accompanying game-action failure (would suggest the day-summary path is failing on its own).
- Seen-set: **115 → 117** fingerprints. No NXDOMAIN flap; host resolving.

Issues downgraded to digest. No urgent ping.

## 01:27Z scan

- `ok: true` — ClickHouse SQL API reachable, query succeeded.
- Window: last 90m. **error=0, warn=0.**
- No error/warn lines in the window; nothing new since last scan.
- Seen-set unchanged at 117 fingerprints (the two 23:21Z lines from the prior scan remain baselined, no re-ping).
- Quiet scan — no digest line, no urgent.

## 02:29Z scan

- `ok: true` — ClickHouse SQL API reachable, query succeeded.
- Window: last 90m. **error=0, warn=0.**
- No error/warn lines in the window; nothing new since last scan.
- Seen-set unchanged at 117 fingerprints (the two 23:21Z lines remain baselined, no re-ping).
- Quiet scan — no digest line, no urgent. No NXDOMAIN flap; host resolving.

## 03:31Z scan

- `ok: true` — ClickHouse SQL API reachable, query succeeded.
- Window: last 90m. **error=0, warn=0.**
- No error/warn lines in the window; nothing new since last scan.
- Seen-set unchanged at 117 fingerprints (the two 23:21Z lines remain baselined, no re-ping).
- Quiet scan — no digest line, no urgent. No NXDOMAIN flap; host resolving.

## 04:32Z scan

- `ok: true` — ClickHouse SQL API reachable, query succeeded.
- Window: last 90m. **error=0, warn=0.**
- No error/warn lines in the window; nothing new since last scan.
- Seen-set steady at 116 fingerprints (the two 23:21Z lines remain baselined, no re-ping).
- Quiet scan — no digest line, no urgent. No NXDOMAIN flap; host resolving.

## 05:34Z scan

- `ok: true` — ClickHouse SQL API reachable, query succeeded.
- Window: last 90m. **error=0, warn=0.**
- No error/warn lines in the window; nothing new since last scan.
- Seen-set steady at 117 fingerprints (the two 23:21Z lines remain baselined, no re-ping).
- Quiet scan — no digest line, no urgent. No NXDOMAIN flap; host resolving.

## 06:35Z scan

- `ok: true` — ClickHouse SQL API reachable, query succeeded.
- Window: last 90m. **error=0, warn=0.**
- No error/warn lines in the window; nothing new since last scan.
- Seen-set steady at 116 fingerprints (the two 23:21Z lines remain baselined, no re-ping).
- Quiet scan — no digest line, no urgent. No NXDOMAIN flap; host resolving.

## 07:37Z scan

- `ok: true` — ClickHouse SQL API reachable, query succeeded.
- Window: last 90m. **error=0, warn=0.**
- No error/warn lines in the window; nothing new since last scan.
- Seen-set steady at 117 fingerprints (the two 23:21Z lines remain baselined, no re-ping).
- Quiet scan — no digest line, no urgent. No NXDOMAIN flap; host resolving.

## 08:39Z scan

- `ok: true` — ClickHouse SQL API reachable, query succeeded.
- Window: last 90m. **error=0, warn=0.**
- No error/warn lines in the window; nothing new since last scan.
- Seen-set steady at 117 fingerprints (the two 23:21Z lines remain baselined, no re-ping).
- Quiet scan — no digest line, no urgent. No NXDOMAIN flap; host resolving.

## 09:41Z scan

- `ok: true` — ClickHouse SQL API reachable, query succeeded.
- Window: last 90m. **error=0, warn=0.**
- No error/warn lines in the window; nothing new since last scan.
- Seen-set steady at 116 fingerprints (the two 23:21Z lines remain baselined, no re-ping).
- Quiet scan — no digest line, no urgent. No NXDOMAIN flap; host resolving.

## 11:25Z scan

- `ok: true` — ClickHouse SQL API reachable, query succeeded.
- Window: last 90m. **error=0, warn=0.**
- No error/warn lines in the window; nothing new since last scan.
- Seen-set steady at 116 fingerprints (the two 23:21Z lines remain baselined, no re-ping).
- Quiet scan — no digest line, no urgent. No NXDOMAIN flap; host resolving.

## 12:47Z scan

- `ok: true` — ClickHouse SQL API reachable, query succeeded.
- Window: last 90m. **error=0, warn=0.**
- No error/warn lines in the window; nothing new since last scan.
- Seen-set steady at 117 fingerprints (the two 23:21Z lines remain baselined, no re-ping).
- Quiet scan — no digest line, no urgent. No NXDOMAIN flap; host resolving.

## 13:49Z scan

- `ok: true` — ClickHouse SQL API reachable, query succeeded.
- Window: last 90m. **error=0, warn=0.**
- No error/warn lines in the window; nothing new since last scan.
- Seen-set steady at 116 fingerprints (the two 23:21Z lines remain baselined, no re-ping).
- Quiet scan — no digest line, no urgent. No NXDOMAIN flap; host resolving.

## 14:30Z scan

- `ok: true` — ClickHouse SQL API reachable, query succeeded.
- Window: last 90m. **error=0, warn=0.**
- No error/warn lines in the window; nothing new since last scan.
- Seen-set steady at 115 fingerprints (the two 23:21Z lines remain baselined, no re-ping).
- Quiet scan — no digest line, no urgent. No NXDOMAIN flap; host resolving.

## 15:32Z scan

- `ok: true` — ClickHouse SQL API reachable, query succeeded.
- Window: last 90m. **error=0, warn=0.**
- No error/warn lines in the window; nothing new since last scan.
- Seen-set steady at 115 fingerprints (the two 23:21Z lines remain baselined, no re-ping).
- Quiet scan — no digest line, no urgent. No NXDOMAIN flap; host resolving.

## 16:34Z scan

- `ok: true` — ClickHouse SQL API reachable, query succeeded.
- Window: last 90m. **error=0, warn=0.**
- No error/warn lines in the window; nothing new since last scan.
- Seen-set steady at 116 fingerprints (the two 23:21Z lines remain baselined, no re-ping).
- Quiet scan — no digest line, no urgent. No NXDOMAIN flap; host resolving.

## 17:35Z scan

- `ok: true` — ClickHouse SQL API reachable, query succeeded.
- Window: last 90m. **error=0, warn=0.**
- No error/warn lines in the window; nothing new since last scan.
- Seen-set steady at 116 fingerprints (the two 23:21Z lines remain baselined, no re-ping).
- Quiet scan — no digest line, no urgent. No NXDOMAIN flap; host resolving.

## 17:46Z scan

- `ok: true` — ClickHouse SQL API reachable, query succeeded.
- Window: last 90m. **error=0, warn=0.**
- No error/warn lines in the window; nothing new since last scan.
- Seen-set steady at 116 fingerprints (the two 23:21Z lines remain baselined, no re-ping).
- Quiet scan — no digest line, no urgent. No NXDOMAIN flap; host resolving.

## 18:36Z scan

- `ok: true` — ClickHouse SQL API reachable, query succeeded.
- Window: last 90m. **error=0, warn=0.**
- No error/warn lines in the window; nothing new since last scan.
- Seen-set steady at 117 fingerprints (the two 23:21Z lines remain baselined, no re-ping).
- Quiet scan — no digest line, no urgent. No NXDOMAIN flap; host resolving.

## 19:49Z scan

- `ok: true` — ClickHouse SQL API reachable, query succeeded.
- Window: last 90m. **error=0, warn=0.**
- No error/warn lines in the window; nothing new since last scan.
- Seen-set steady at 116 fingerprints (the two 23:21Z lines remain baselined, no re-ping).
- Quiet scan — no digest line, no urgent. No NXDOMAIN flap; host resolving.

## 20:31Z scan

- `ok: true` — ClickHouse SQL API reachable, query succeeded.
- Window: last 90m. **error=0, warn=0.**
- No error/warn lines in the window; nothing new since last scan.
- Seen-set steady at 117 fingerprints (the two 23:21Z lines remain baselined, no re-ping).
- Quiet scan — no digest line, no urgent. No NXDOMAIN flap; host resolving.

## 21:33Z scan

- `ok: true` — ClickHouse SQL API reachable, query succeeded.
- Window: last 90m. **error=0, warn=0.**
- No error/warn lines in the window; nothing new since last scan.
- Seen-set steady at 116 fingerprints (the two 23:21Z lines remain baselined, no re-ping).
- Quiet scan — no digest line, no urgent. No NXDOMAIN flap; host resolving.

## 23:21Z scan

- `ok: true` — ClickHouse SQL API reachable, query succeeded.
- Window: last 90m. **error=0, warn=0.**
- No error/warn lines in the window; nothing new since last scan.
- Seen-set steady at 117 fingerprints (the two 23:21Z lines remain baselined, no re-ping).
- Quiet scan — no digest line, no urgent. No NXDOMAIN flap; host resolving.

— Marlow
