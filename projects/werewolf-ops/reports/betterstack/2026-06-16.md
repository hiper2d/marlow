# Werewolf app logs (Betterstack) — 2026-06-16

## Scan 00:46Z

90-minute window (~23:16Z -15 → 00:46Z -16). **error=0, warn=0.** Handler `ok:true` — the ClickHouse host resolves and queries cleanly, so the -14 `eu-fsn-3` NXDOMAIN flap stays cleared (no recurrence into -16). Nothing new since the last scan; the same six fingerprints carry in the seen-set (still incl. the -11 standalone `Game action failed: L` line `b9150664…`, `baselined:false`), so no re-alert. No alert. Zero-error baseline holds — first window of the -16 UTC day.

Coverage caveat: the prior scan was -15 20:27Z, so the ~20:27Z → 23:16Z stretch fell between windows unscanned. Game-action-failure watch unchanged — still no fourth occurrence in any covered window since -11.

## Scan 16:48Z

90-minute window (~15:18Z → 16:48Z). **error=0, warn=0.** Handler `ok:true` — ClickHouse host still resolving and querying cleanly, NXDOMAIN flap stays cleared. Nothing new since the last scan; same six fingerprints carry in the seen-set (`baselined:false`), no re-alert. No alert, empty digest. Zero-error baseline holds.

Coverage caveat: prior scan was 00:46Z, so the ~02:16Z → 15:18Z stretch fell between windows unscanned (the hourly cron didn't fire across the overnight/morning local-sleep gap). Game-action-failure watch unchanged — no fourth occurrence in any covered window since -11.

## Scan 18:21Z

90-minute window (~16:51Z → 18:21Z). **error=0, warn=0.** Handler `ok:true` — ClickHouse host still resolving and querying cleanly, NXDOMAIN flap stays cleared. Nothing new since the last scan; same six fingerprints carry in the seen-set (`baselined:false`), no re-alert. No alert, empty digest. Zero-error baseline holds. Window contiguous with the 16:48Z scan (~3m gap, no coverage hole).

Game-action-failure watch unchanged — no fourth occurrence in any covered window since -11.

## Scan 19:23Z

90-minute window (~17:53Z → 19:23Z). **error=0, warn=0.** Handler `ok:true` — ClickHouse host still resolving and querying cleanly, NXDOMAIN flap stays cleared. Nothing new since the last scan; same six fingerprints carry in the seen-set (`baselined:false`), no re-alert. No alert, empty digest. Zero-error baseline holds. Window contiguous with the 18:21Z scan (~1h gap, fully covered).

Game-action-failure watch unchanged — no fourth occurrence in any covered window since -11.

## Scan 20:24Z

90-minute window (~18:54Z → 20:24Z). **error=0, warn=0.** Handler `ok:true` — ClickHouse host still resolving and querying cleanly, NXDOMAIN flap stays cleared. Nothing new since the last scan; same six fingerprints carry in the seen-set (`baselined:false`), no re-alert. No alert, empty digest. Zero-error baseline holds. Window contiguous with the 19:23Z scan (~1h gap, fully covered).

Game-action-failure watch unchanged — no fourth occurrence in any covered window since -11.

## Scan 21:27Z

90-minute window (~19:57Z → 21:27Z). **error=0, warn=0.** Handler `ok:true` — ClickHouse host still resolving and querying cleanly, NXDOMAIN flap stays cleared. Nothing new since the last scan; same six fingerprints carry in the seen-set (`baselined:false`), no re-alert. No alert, empty digest. Zero-error baseline holds. Window contiguous with the 20:24Z scan (~1h gap, fully covered).

Game-action-failure watch unchanged — no fourth occurrence in any covered window since -11.

## Scan 22:28Z

90-minute window (~20:58Z → 22:28Z). **error=0, warn=0.** Handler `ok:true` — ClickHouse host still resolving and querying cleanly, NXDOMAIN flap stays cleared. Nothing new since the last scan; same six fingerprints carry in the seen-set (`baselined:false`), no re-alert. No alert, empty digest. Zero-error baseline holds. Window contiguous with the 21:27Z scan (~1h gap, fully covered).

Game-action-failure watch unchanged — no fourth occurrence in any covered window since -11.

## Scan 23:30Z

90-minute window (~22:00Z → 23:30Z). **error=0, warn=0.** Handler `ok:true` — ClickHouse host still resolving and querying cleanly, NXDOMAIN flap stays cleared. Nothing new since the last scan; same six fingerprints carry in the seen-set (`baselined:false`), no re-alert. No alert, empty digest. Zero-error baseline holds. Window contiguous with the 22:28Z scan (~1h gap, fully covered).

Game-action-failure watch unchanged — no fourth occurrence in any covered window since -11.

— Marlow
