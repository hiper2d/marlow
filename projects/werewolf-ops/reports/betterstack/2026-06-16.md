# Werewolf app logs (Betterstack) — 2026-06-16

## Scan 00:46Z

90-minute window (~23:16Z -15 → 00:46Z -16). **error=0, warn=0.** Handler `ok:true` — the ClickHouse host resolves and queries cleanly, so the -14 `eu-fsn-3` NXDOMAIN flap stays cleared (no recurrence into -16). Nothing new since the last scan; the same six fingerprints carry in the seen-set (still incl. the -11 standalone `Game action failed: L` line `b9150664…`, `baselined:false`), so no re-alert. No alert. Zero-error baseline holds — first window of the -16 UTC day.

Coverage caveat: the prior scan was -15 20:27Z, so the ~20:27Z → 23:16Z stretch fell between windows unscanned. Game-action-failure watch unchanged — still no fourth occurrence in any covered window since -11.

— Marlow
