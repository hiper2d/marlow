# Betterstack app-log monitor — 2026-06-22

## Scan 01:23:43Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 6 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves and queries, no NXDOMAIN flap. App quiet, baseline holds.

## Scan 02:26:13Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 6 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves/queries, no NXDOMAIN flap. App quiet, baseline holds.

## Scan 11:16:27Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 6 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves/queries, no NXDOMAIN flap. App quiet, baseline holds.

## Scan 14:24:33Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 6 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves/queries, no NXDOMAIN flap. App quiet, baseline holds.

## Scan 15:25:59Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 6 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves/queries, no NXDOMAIN flap. App quiet, baseline holds.

## Scan 16:27:35Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 6 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves/queries, no NXDOMAIN flap. App quiet, baseline holds.

## Scan 17:29:25Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 6 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves/queries, no NXDOMAIN flap. App quiet, baseline holds.

## Scan 18:31:38Z

- Window: last 90m
- error=2, warn=0
- New error lines this scan (seen-set 6 → 8 fingerprints):
  - `2026-06-22 17:42:43.891000  [error] Game action failed: I`
  - `2026-06-22 17:42:42.645000  [error] Game action failed: I`
- Same message, ~1.2s apart → one logical event (a single game's action failing, logged on retry).

ClickHouse SQL API reachable (`ok: true`), no NXDOMAIN flap. **Handler flagged both as `urgent` (presence-based: any new error fingerprint is urgent by design). Downgraded to digest, no urgent ping** — `Game action failed: <char>` is the known recurring game-action-failure class already standing in the baseline (the `K`/`L` siblings tolerated as green in the -21 report). The trailing char is a per-occurrence token (game/action id), so each fresh occurrence reads as a "new" fingerprint. This is one in-game action failing — non-systemic, monitor_health's broken-game territory — not app-wide breakage or a public 5xx. Per the hard "urgent = blocking only" constraint, it goes to the digest, not Telegram urgent.

## Scan 19:36:35Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 8 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves/queries, no NXDOMAIN flap. App quiet; the 18:31 `Game action failed: I` pair did not recur.

## Scan 20:38:07Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 8 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves/queries, no NXDOMAIN flap. App quiet; the 18:31 `Game action failed: I` pair still not recurring.

## Scan 21:40:46Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 8 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves/queries, no NXDOMAIN flap. App quiet; the 18:31 `Game action failed: I` pair still not recurring.

## Scan 22:21:51Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 8 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves/queries, no NXDOMAIN flap. App quiet; the 18:31 `Game action failed: I` pair still not recurring.

## Scan 23:23:22Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 8 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves/queries, no NXDOMAIN flap. App quiet; the 18:31 `Game action failed: I` pair still not recurring.

## Issues this run

- None new this scan — all green. (Earlier today: 2 `error` lines at 18:31, both `Game action failed: I` — one logical event 17:42:42–43Z, classified as the known recurring game-action-failure class and routed to digest, not urgent. Not recurring since.)

— Marlow
