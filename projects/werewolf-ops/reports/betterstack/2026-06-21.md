# Betterstack app-log monitor — 2026-06-21

## Scan 02:10:55Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 6 fingerprints intact, incl. the standing `Game action failed: K`/`L` lines).

ClickHouse SQL API reachable (`ok: true`) — host resolves and queries, no NXDOMAIN flap. App quiet, baseline holds.

## Scan 12:59:26Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 6 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves and queries, no NXDOMAIN flap. App quiet, baseline holds.

## Scan 16:21:01Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 6 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves and queries, no NXDOMAIN flap. App quiet, baseline holds.

## Scan 17:22:43Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 6 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves and queries, no NXDOMAIN flap. App quiet, baseline holds.

## Scan 18:34:47Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 6 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves and queries, no NXDOMAIN flap. App quiet, baseline holds.

## Scan 19:37:46Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 6 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves and queries, no NXDOMAIN flap. App quiet, baseline holds.

## Scan 21:19:20Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 6 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves and queries, no NXDOMAIN flap. App quiet, baseline holds.

## Scan 22:21:51Z

- Window: last 90m
- error=0, warn=0
- No error/warn lines in the window.
- No new error/warn lines since last scan (seen-set of 6 fingerprints intact).

ClickHouse SQL API reachable (`ok: true`) — host resolves and queries, no NXDOMAIN flap. App quiet, baseline holds.

## Issues this run

None — all green (all eight scans).

— Marlow
