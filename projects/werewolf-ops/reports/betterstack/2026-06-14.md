# Werewolf app logs (Betterstack) — 2026-06-14

## Scan 01:06Z

90-minute window (~23:36Z-13 → 01:06Z-14). **error=0, warn=0.** First scan of the UTC day; nothing new since the -13 20:04Z scan, `baselined:false`. The same six fingerprints carry in the seen-set (still incl. the -11 standalone `Game action failed: L` line `b9150664…`), so no re-alert. No alert. Zero-error baseline carries clean across all of -12 and -13 into -14. Coverage gap: ~5h since the -13 20:04Z scan (laptop sleep), so ~20:04Z-13 → ~23:36Z-13 fell between windows unobserved. Game-action-failure watch unchanged — still no fourth occurrence in any covered window since -11.

## Scan 06:19Z

90-minute window (~04:49Z → 06:19Z). **error=0, warn=0.** Nothing new since the 01:06Z scan; same six fingerprints carry in the seen-set, no re-alert. No alert. Clean baseline holds into a third UTC day. Coverage gap: ~5h since the 01:06Z scan (laptop sleep), so ~01:06Z → ~04:49Z fell between windows unobserved. Game-action-failure watch unchanged — no fourth occurrence in any covered window since -11.

## Scan 11:09Z — CONNECTION FAILED (stale ClickHouse endpoint)

**Handler `ok:false` — could not connect.** `NameResolutionError` on the configured ClickHouse host `eu-fsn-3-connect.betterstackdata.com`. Confirmed NXDOMAIN across the local resolver, 8.8.8.8, and 1.1.1.1 — the host genuinely no longer exists in DNS. Not a transient timeout, not a local-resolver quirk: general DNS works, `betterstackdata.com` root resolves, and a sibling connect host (`eu-nbg-2-connect.betterstackdata.com`) resolves fine. BetterStack retired/migrated the `eu-fsn-3` SQL-API endpoint sometime between the clean 06:19Z scan and now (~5h window).

This is a **stale-config** issue, not a Marlow code bug — the handler correctly reads `BETTERSTACK_CH_HOST` from the launchd plist (`com.marlow.tick.plist`); the value just points at a dead host. The current connect host lives behind Alex's BetterStack login (Telemetry → Sources → the source → Connect / SQL API) and can't be guessed safely, so this is out of self-heal scope → escalated to Alex (urgent). **No log scan performed this tick; app-level error watch is dark until the host is updated.** Game-action-failure watch carry-state intact (seen-set untouched, no scan ran).

## Scan 20:22Z — RECOVERED (connection restored)

**Handler `ok:true` — connection restored.** 90-minute window (~18:52Z → 20:22Z). **error=0, warn=0.** The ClickHouse host resolves and queries again, so the morning's `eu-fsn-3` outage is over — either the endpoint came back or the plist `BETTERSTACK_CH_HOST` was updated; in either case the SQL API is reachable. The seen-set carries the **same six fingerprints** intact (still incl. the -11 standalone `Game action failed: L` line `b9150664…`, `baselined:false`), so the ~9h dark window (06:19Z clean → 11:09Z fail → here) cost no carry-state — the game-action-failure dedup is uninterrupted. No new error/warn lines, no alert. Zero-error baseline holds into the back half of -14 after the gap. Coverage caveat: ~18:52Z onward is observed; the 11:09Z→18:52Z stretch fell in the outage/sleep window and went unscanned. Game-action-failure watch unchanged — still no fourth occurrence in any covered window since -11.

— Marlow
