# Werewolf activity — 2026-06-13

Snapshot `2026-06-13T14:22:48Z` (prior: `2026-06-12T18:46:51Z`, ~19.6h gap —
daily cadence holding, slot drifted back into the morning window this time).

```
  Users    124 total  (123 free / 0 api / 1 paid)
    new:   2 today · 14 7d · 39 30d
  Games    32 live  (TTL 30d)
    new:   1 today · 12 7d · 32 30d  (1 by today's new users)
  Burn     $10.66 live cumulative
    cost of games started: $0.01 today · $7.13 7d · $10.66 30d
    spent since last snapshot (19.6h): $0.00  ⚠ (some games expired out of window)
  Spend    $7.91 MTD (2026-06): $6.83 free / $0.99 api / $0.0006 paid
```

## What moved vs last snapshot

Users 121 → 124 (+3 across the window): two signups today
(`17330004bc@gmail.com`, `parsifalross@gmail.com`) plus one more late on -12
after the 18:46Z snapshot. 30d new ticked back up 37 → 39, so the monthly
intake slope firmed after yesterday's flat-to-soft read. Tier split flickered
again — the lone `api` slot dropped to 0 and a `paid` user appeared (123 free /
0 api / 1 paid). The paid flag is the headline-looking move, but it's hollow:
`paid` MTD revenue is still $0.0006, so this is a tier label, not income — a
user sitting on the paid tier who has spent essentially nothing.

**The signup→immediate-game funnel is back.** After -12's first miss in four
days, today's only created game (`Dracula`, $0.0062, still in WELCOME) is owned
by `parsifalross` — a user who signed up *today*. `created_today_by_new_users:
1`. So the funnel resumed on a fresh signup; the one quiet day reads as noise,
not a turn. Note the flip in who's carrying the line: the prior three days were
`biostellarllc` (one returning power user) owning every created game; today
that user is absent from the created line and a brand-new user takes it
instead.

**Burn is unreadable this window — TTL artefact, not a quiet day.** `daily_burn`
reports $0.00 over 19.6h, but the raw delta is *negative* (−$0.39) with
`expired_games_suspected: true`. Live cumulative fell $11.05 → $10.66 and the
live count dropped 33 → 32, so older games aged out of the trailing-30-day
window faster than today's tiny new burn ($0.01 from the one new Dracula game)
accrued. This is exactly the failure mode the -12 note flagged building: the
cost line had detached from the creation line (burn coming from long-running
games accruing), and now that those long games are starting to hit their 30d
TTL, the live-cumulative metric reads them *leaving* and nets to zero/negative.
True daily spend this window is some small positive number masked by expiry —
correlate against `monitor_keys`/`scrape_stats` provider deltas for the real
figure, not this snapshot.

MTD spend $7.68 → $7.91 (+$0.23), again almost entirely the `free` slice
($6.61 → $6.83) — our cost on free-tier games. `api` flat at $0.99, `paid` flat
at $0.0006. June income remains effectively zero despite the new paid-tier
label.

Digest-only: steady signups (the funnel resumed on a new user), one new
created game, and the burn series has now entered the regime where the 30d TTL
masks live spend — so going forward the provider-balance monitors, not this
live-cumulative number, are the trustworthy daily-burn read. Nothing to alarm
on; the budget watch owns spend alerts.

— Marlow
