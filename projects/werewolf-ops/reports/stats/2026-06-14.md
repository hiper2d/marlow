# Werewolf activity — 2026-06-14

Snapshot `2026-06-14T18:48:51Z`. An earlier snapshot already persisted today at
`14:34:07Z` (the `collect_stats` tick re-queued after a transient Claude
session-limit hit, so the `report` ran but the markdown didn't land); the two
intraday snapshots are identical (127 users, $10.12 live, flat), so the
day-over-day read below is against yesterday's `-13` 14:22Z snapshot.

```
  Users    127 total  (126 free / 0 api / 1 paid)
    new:   1 today · 14 7d · 40 30d
  Games    30 live  (TTL 30d)
    new:   0 today · 9 7d · 29 30d
  Burn     $10.12 live cumulative
    cost of games started: $0.00 today · $6.56 7d · $10.01 30d
    spent since last snapshot (4.2h): $0.00
  Spend    $8.25 MTD (2026-06): $7.16 free / $0.99 api / $0.0151 paid
```

## What moved vs last snapshot

Users 124 → 127 (+3 across the ~28h window): one signup today
(`xufenghuaxu14@gmail.com`) plus two more late on -13 after the 14:22Z snapshot.
30d new ticked up 39 → 40, so the monthly intake slope is still firming. Tier
split steady — 126 free / 0 api / 1 paid, same shape as -13 (the lone `paid`
user persists, the `api` slot still empty).

**No games created today, and the live count keeps draining.** `created.today:
0`, `created_today_by_new_users: 0` — today's one signup didn't start a game, so
the signup→immediate-game funnel misses again (it last fired -13 on
`parsifalross`). Live game count fell 32 → 30; the 7d created window dropped 12
→ 9 and 30d 32 → 29, both because older games are aging off their trailing
windows faster than new ones replace them. Creation is genuinely quiet, not just
TTL-masked: zero new games today is a real read.

**Burn unreadable from this snapshot — same TTL artefact as -13.** Live
cumulative fell $10.66 → $10.12 (−$0.54) as games aged out of the 30d window.
The handler's `daily_burn` reports $0.00 with `raw_delta 0.0` and
`expired_games_suspected: false`, but that's only because the immediately-prior
snapshot it differences against is *today's own* 14:34Z read — 4.2h of genuine
flat, not the full day. Across the day the true spend is some small positive
number masked by expiry. Use `monitor_keys`/`scrape_stats` provider deltas for
the real daily-burn figure, not this live-cumulative line.

MTD spend $7.91 → $8.25 (+$0.34), again almost all the `free` slice
($6.83 → $7.16, +$0.33) — our cost on free-tier games. `api` flat at $0.99.
`paid` ticked $0.0006 → $0.0151 — the lone paid user spent ~1.4 cents, so June
income moves from "effectively zero" to "still effectively zero." The paid-tier
label remains a label, not revenue.

Digest-only: steady signups (40 over 30d), zero new games today (funnel quiet),
and the burn series stays in the TTL-masked regime where the provider-balance
monitors are the trustworthy daily-spend read. Nothing to alarm on; the budget
watch owns spend alerts.

— Marlow
