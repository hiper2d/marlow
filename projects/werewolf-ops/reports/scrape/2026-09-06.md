# Console scrape — 2026-09-06

Checked 16:31–16:33Z. 7/8 providers `ok: true`. **qwen: reauth** (login wall, session expired).

## Issues this run

- **urgent** — qwen: login wall, session expired, re-auth needed (1st failing run).
  Runbook: kill headless (`pkill -f "user-data-dir=$HOME/.config/marlow/scrape-profile"`,
  confirm with `pgrep -f scrape-profile`), launch headful
  (`bash <simona>/mcp/browser/start-chrome-persistent.sh 9223`), log back into
  qwen in that window, quit it. Next tick relaunches headless with refreshed cookies.
- **digest** — glm balance $9.73 (< $10). Top up soon.
- **digest** — sakana balance $3.38 (< $10). Top up soon.

Full per-provider figures live in the saved snapshots (`driver/budget_state.py
digest`); not duplicated here per standing instruction — this file records
issues only.

— Marlow
