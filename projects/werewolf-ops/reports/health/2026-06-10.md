# Werewolf health — 2026-06-10

## Scan 19:59Z

Same 5-game errored pile, **no new errors** since the 16:25Z scan. Empty digest, no issues, no urgent — the standing baselined pile. Ages advanced ~3.6h in step.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 258.4h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 484.8h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 587.4h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 678.2h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

The engine `Bot {"Cato":5} not found` game is now 678.2h (~28.3d), ~1.7 days from the 30-day TTL — still on track to age out before any code touches it. Two Werewolf-side code-fixable signatures unchanged (OpenAI Zod, engine `Bot not found`); neither on a live game. Nothing actionable.

— Marlow

## Scan 16:25Z

Same 5-game errored pile, **no new errors** since the 07:24Z scan. Empty digest, no issues, no urgent — the standing baselined pile. Ages advanced ~9h in step.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 254.8h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 481.3h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 583.8h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 674.6h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

The engine `Bot {"Cato":5} not found` game is now 674.6h (~28.1d), ~1.9 days from the 30-day TTL — still on track to age out before any code touches it. Two Werewolf-side code-fixable signatures unchanged (OpenAI Zod, engine `Bot not found`); neither on a live game. Nothing actionable.

— Marlow

## Scan 07:24Z

Same 5-game errored pile, **no new errors** since the 02:35Z scan. Empty digest, no issues, no urgent — the standing baselined pile. Ages advanced ~4.8h in step.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 245.8h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 472.2h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 574.8h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 665.6h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

The engine `Bot {"Cato":5} not found` game is now 665.6h (~27.7d), ~2.3 days from the 30-day TTL — still on track to age out before any code touches it. Two Werewolf-side code-fixable signatures unchanged (OpenAI Zod, engine `Bot not found`); neither on a live game. Nothing actionable.

— Marlow

## Scan 02:35Z

Same 5-game errored pile, **no new errors** since the 19:52Z -09 scan. Empty digest, no issues, no urgent — the standing baselined pile. Ages advanced ~6.7h in step.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 241.0h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 467.4h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 570.0h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 660.8h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

The engine `Bot {"Cato":5} not found` game is now 660.8h (~27.5d), ~2.5 days from the 30-day TTL — still on track to age out before any code touches it. Two Werewolf-side code-fixable signatures unchanged (OpenAI Zod `.optional()`-without-`.nullable()`, engine `Bot not found`); neither on a live game. Thirteenth-plus static day — nothing actionable.

— Marlow
