# Werewolf health — 2026-06-08

## Scan 07:35Z

Same 5-game errored pile (live ≤30d set). **No new errors since last scan** (00:42Z -08) — the standing pile, all previously baselined. No issues, no urgent, no digest line.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 198.0h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 424.4h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 527.0h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 617.8h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

Ages advanced ~6.9h since the 00:42Z scan, in step (the OpenAI Zod entry still carries a null `error_at`/age). The engine `Bot {"Cato":5} not found` game is now 617.8h (~25.7d), ~4.3 days from the 30-day TTL that retires it from the live set — it will likely age out before the next code change touches it. Still the same two Werewolf-side code-fixable signatures (OpenAI Zod `.optional()`-without-`.nullable()`, engine `Bot not found`); neither has touched a live game. Seventh-plus static day — nothing actionable.

— Marlow

## Scan 17:07Z

Same 5-game errored pile, **no new errors** since 07:35Z. Empty digest, no issues, no urgent. Ages advanced ~9.5h in step.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 207.5h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 434.0h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 536.5h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 627.3h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

The engine `Bot {"Cato":5} not found` game is now 627.3h (~26.1d), ~3.9 days from the 30-day TTL — still on track to age out before any code touches it. Two Werewolf-side code-fixable signatures unchanged (OpenAI Zod, engine `Bot not found`); neither on a live game. Static day continues — nothing actionable.

— Marlow

## Scan 20:03Z

Same 5-game errored pile, **no new errors** since 17:07Z. Empty digest, no issues, no urgent. Ages advanced ~2.9h in step.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 210.5h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 436.9h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 539.5h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 630.3h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

The engine `Bot {"Cato":5} not found` game is now 630.3h (~26.3d), ~3.7 days from the 30-day TTL — still on track to age out before any code touches it. Two Werewolf-side code-fixable signatures unchanged (OpenAI Zod, engine `Bot not found`); neither on a live game. Eighth-plus static day — nothing actionable.

— Marlow
