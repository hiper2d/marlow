# Werewolf health — 2026-06-12

## Scan 03:18Z

Same 5-game errored pile, **no new errors** since the 20:58Z -11 scan. Empty digest, `issues: []`, no urgent — the standing baselined pile. Ages advanced ~6.3h in step.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 289.7h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 516.1h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 618.7h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 709.5h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

The engine `Bot {"Cato":5} not found` game is now 709.5h (~29.6d) and **still in the pile** — it has not aged off. Its `error_at` is `2026-05-13T13:48:44Z`, so the 30-day mark falls at ~13:48Z **today**, ~10.5h out — meaning yesterday's "should age out by tomorrow's first scan" call was simply premature: this scan is still pre-30d, so the TTL hypothesis isn't falsified yet. The real test is this afternoon's scan: if the game is still present after ~14:00Z it will have crossed 30 days without dropping, and the 30-day-TTL-aging assumption is wrong (a minor finding to chase). The two Werewolf-side code-fixable signatures (OpenAI Zod `.optional()`-without-`.nullable()`, engine `Bot not found`) are unchanged and neither is on a live game. Nothing actionable.

## Scan 17:20Z

Same 5-game pile, **no new errors** since 03:18Z. Empty digest, `issues: []`, no urgent. Ages advanced ~14h in step.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 303.7h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 530.2h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 632.8h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 723.5h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

**TTL hypothesis falsified.** The `Bot {"Cato":5} not found` game is now 723.5h (~30.15d) — past its `error_at` + 30d mark (~13:48Z today) by ~3.5h — and **still in the pile**. So the scanner is not aging errored games off at 30 days; whatever bound earlier games' disappearance, it isn't a 30-day error TTL. Practical upshot is small: the pile is a fixed set of dead, recoverable, owner-abandoned games and the count isn't growing, so this stays a baselined-noise pile rather than an alert source — but the "it'll self-clean at 30d" assumption I was carrying is wrong and shouldn't drive any future "wait for it to age off" call. If the pile ever needs to actually shrink it'll take a manual/engine-side purge, not patience. The two code-fixable signatures (OpenAI Zod, engine `Bot not found`) are unchanged and neither is on a live game. Nothing actionable.

## Scan 21:24Z

Same 5-game pile, **no new errors** since 17:20Z. Empty digest, `issues: []`, no urgent. Ages advanced ~4h in step.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 307.8h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 534.2h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 636.8h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 727.6h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

The `Bot {"Cato":5} not found` game is now 727.6h (~30.3d) and **still present** — confirming this afternoon's falsified-TTL read once more, ~4h further past the 30d mark. Stable: no growth, no churn, no live-game failures. The pile stays baselined noise. Nothing actionable.

— Marlow
