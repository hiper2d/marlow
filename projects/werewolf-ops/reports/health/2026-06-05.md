# Werewolf health — 2026-06-05

## Scan 04:06Z

5 games currently carrying an error (live ≤30d set). **No new errors since last scan** (21:21Z -04) — the standing pile, all previously baselined. No urgent, no digest line.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 122.5h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 348.9h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 451.5h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 542.3h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

Same five carried over from -04, ages advanced ~7h in step with the gap (the OpenAI Zod one still has a null `error_at`/age). All recoverable provider-parse / schema failures on stalled abandoned games — not a live failure rate. The pile has now been static for over two days; the oldest (the engine-side `Bot {"Cato":5} not found`) is at 542h, closing on the 30-day TTL that will retire it from the live set. The OpenAI Zod `.optional()`-without-`.nullable()` and that engine `Bot not found` remain the two that look Werewolf-side code-fixable rather than provider flakiness; flag if either recurs on a *live* game.

## Scan 07:31Z

Same five, no new errors since the 04:06Z scan — ages advanced ~3.4h in step. Nothing started or failed in between. No urgent, no digest line.

| Theme | State | Age | Provider / failure |
|---|---|---|---|
| Star Wars | WELCOME | 125.9h | DeepSeek — JSON parse |
| Hunger Games | WELCOME | 352.4h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | 454.9h | Grok — JSON parse |
| Hunger Games | VOTE | 545.7h | engine — `Bot {"Cato":5} not found` |
| Hunger Games | NIGHT | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

The engine `Bot {"Cato":5} not found` game is at 545.7h (~22.7d) — about a week from the 30-day TTL that retires it from the live set. Pile static three-plus days now; no live-game involvement.

## Scan 15:09Z

Same five, no new errors since the 07:31Z scan — ages advanced ~7.6h in step. No urgent, no digest line.

| Theme | State | Age | Provider / failure |
|---|---|---|---|
| Star Wars | WELCOME | 133.6h | DeepSeek — JSON parse |
| Hunger Games | WELCOME | 360.0h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | 462.6h | Grok — JSON parse |
| Hunger Games | VOTE | 553.3h | engine — `Bot {"Cato":5} not found` |
| Hunger Games | NIGHT | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

The engine `Bot {"Cato":5} not found` game is at 553.3h (~23.1d), ~7 days from the 30-day TTL. Pile unchanged into a third-plus static day; no live failures across all three of today's scans.

## Scan 19:22Z

Same five, no new errors since the 15:09Z scan — ages advanced ~4.2h in step. No urgent, no digest line.

| Theme | State | Age | Provider / failure |
|---|---|---|---|
| Star Wars | WELCOME | 137.8h | DeepSeek — JSON parse |
| Hunger Games | WELCOME | 364.2h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | 466.8h | Grok — JSON parse |
| Hunger Games | VOTE | 557.6h | engine — `Bot {"Cato":5} not found` |
| Hunger Games | NIGHT | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

The engine `Bot {"Cato":5} not found` game is at 557.6h (~23.2d), ~7 days from the 30-day TTL. Four clean scans today (04:06 / 07:31 / 15:09 / 19:22Z), pile static into a third-plus day, no live failures.

— Marlow
