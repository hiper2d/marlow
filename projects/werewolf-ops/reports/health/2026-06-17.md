# Werewolf health — 2026-06-17

## Scan 01:12Z

**Pile still 2 — flat since the -16 18:41Z scan (~6.5h).** Same two provider-parse/validation games, ages advanced ~6.5h in step. **No new errors** (`issues: []`, empty digest, no urgent).

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 407.6h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 634.0h | Mistral — response validation failed |

Both inside the 30d TTL window and tracking toward self-clean now that the Grok game's -16 self-clean confirmed the TTL fires. **Mistral is now 634.0h ≈ 26.4d — clears 30d (720h) in ~3.6 days (~-20), the next falsified-TTL datapoint to watch.** Star Wars/DeepSeek trails at 407.6h (~17.0d). Both code-fixable signatures (OpenAI Zod, engine `Bot not found`) remain at zero games; neither error sits on a live game; all recoverable. Nothing actionable this run.

## Scan 06:41Z

**Pile still 2 — flat, ages advanced ~5.5h in step.** Same two provider-parse/validation games. **No new errors** (`issues: []`, empty digest, no urgent).

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 413.1h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 639.5h | Mistral — response validation failed |

**Mistral now 639.5h ≈ 26.6d — ~3.4 days from the 720h TTL boundary (~-20).** Star Wars/DeepSeek at 413.1h (~17.2d). Both code-fixable signatures (OpenAI Zod, engine `Bot not found`) still zero; neither error on a live game; all recoverable. Nothing actionable.

## Scan 13:23Z

**Pile still 2 — flat, ages advanced ~6.7h in step.** Same two provider-parse/validation games. **No new errors** (`issues: []`, empty digest, no urgent).

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 419.8h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 646.2h | Mistral — response validation failed |

**Mistral now 646.2h ≈ 26.9d — ~3.1 days from the 720h TTL boundary (~-20).** Star Wars/DeepSeek at 419.8h (~17.5d). Both code-fixable signatures (OpenAI Zod, engine `Bot not found`) still zero; neither error on a live game; all recoverable. Nothing actionable.

## Scan 18:41Z

**Pile still 2 — flat, ages advanced ~5.3h in step.** Same two provider-parse/validation games. **No new errors** (`issues: []`, empty digest, no urgent).

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 425.1h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 651.5h | Mistral — response validation failed |

**Mistral now 651.5h ≈ 27.1d — ~2.9 days from the 720h TTL boundary (~-20).** Star Wars/DeepSeek at 425.1h (~17.7d). Both code-fixable signatures (OpenAI Zod, engine `Bot not found`) still zero; neither error on a live game; all recoverable. Nothing actionable.

— Marlow
