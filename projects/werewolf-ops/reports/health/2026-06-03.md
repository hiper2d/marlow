# Werewolf health — 2026-06-03

## Scan 01:47Z

5 games currently carrying an error (live ≤30d set). **No new errors since last scan** — the standing pile, all previously baselined. No urgent, no digest line.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 72.2h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 298.6h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 401.2h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 492.0h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

All five are recoverable provider-parse / schema failures, not infra. The pattern holds from prior scans: LLM response-parse failures concentrated in DeepSeek/Grok JSON and Mistral/OpenAI schema validation — a game-reliability signal, consistent with what the README anticipates. The OpenAI Zod `.optional()`-without-`.nullable()` and the engine-side `Bot not found` are the two that look code-fixable on the Werewolf side rather than provider flakiness.

## Scan 18:00Z

Same 5 errored games, all previously baselined — **no new errors since 01:47Z**. Ages advanced ~16h in step with the gap (Star Wars 88.4h, Hunger Games 314.8h, werewolf 417.4h, Hunger Games/VOTE 508.2h; the OpenAI Zod one has a null `error_at`/age). No urgent, no digest line. The pile is static — these are stalled games no one has retried, not a live failure rate.

## Scan 21:09Z

Same 5 errored games, all previously baselined — **no new errors since 18:00Z**. Ages advanced ~3h in step with the gap (Star Wars 91.6h, Hunger Games/Mistral 318.0h, werewolf 420.6h, Hunger Games/VOTE 511.3h; the OpenAI Zod one still null `error_at`/age). No urgent, no digest line. Third clean scan of the day — the standing pile has not moved all day, confirming these are stalled abandoned games, not an active failure stream.

— Marlow
