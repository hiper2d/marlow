# Werewolf health — 2026-06-06

## Scan 02:34Z

5 games currently carrying an error (live ≤30d set). **No new errors since last scan** (19:22Z -05) — the standing pile, all previously baselined. No urgent, no digest line.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 145.0h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 371.4h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 474.0h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 564.8h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

Same five carried over from -05, ages advanced ~7.2h in step with the gap (the OpenAI Zod one still has a null `error_at`/age). All recoverable provider-parse / schema failures on stalled abandoned games — not a live failure rate. The engine `Bot {"Cato":5} not found` game is at 564.8h (~23.5d), ~6.5 days from the 30-day TTL that retires it from the live set. The OpenAI Zod `.optional()`-without-`.nullable()` and that engine `Bot not found` remain the two that look Werewolf-side code-fixable rather than provider flakiness; flag if either recurs on a *live* game. Pile static into a fourth-plus day; no live-game involvement.

## Scan 07:22Z

Same 5 games, no new errors since 02:34Z. Standing pile only — no urgent, no digest line.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 149.8h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 376.2h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 478.8h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 569.6h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

Ages advanced ~4.8h in step with the gap; OpenAI Zod entry still null `error_at`/age. The engine `Bot not found` game now 569.6h (~23.7d), ~6.3 days from the 30-day TTL retirement. No change in character — recoverable parse/schema failures on long-abandoned games, no live involvement. The two Werewolf-side-fixable candidates (OpenAI Zod, engine `Bot not found`) unchanged; flag only if either recurs on a live game.

## Scan 17:03Z

Same 5 games, no new errors since 07:22Z. Standing pile only — no urgent, no digest line.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 159.5h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 385.9h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 488.5h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 579.3h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

Ages advanced ~9.7h in step with the gap; OpenAI Zod entry still null `error_at`/age. The engine `Bot not found` game now 579.3h (~24.1d), ~5.9 days from the 30-day TTL retirement. Unchanged in character — recoverable parse/schema failures on long-abandoned games, no live involvement. Third-plus static day; the two Werewolf-side-fixable candidates (OpenAI Zod, engine `Bot not found`) flag only if either recurs on a live game.

## Scan 19:43Z

Same 5 games, no new errors since 17:03Z. Standing pile only — no urgent, no digest line.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 162.1h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 388.5h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 491.1h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 581.9h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

Ages advanced ~2.6h in step with the gap; OpenAI Zod entry still null `error_at`/age. The engine `Bot not found` game now 581.9h (~24.2d), ~5.8 days from the 30-day TTL retirement. No change in character — recoverable parse/schema failures on long-abandoned games, no live involvement. Fourth scan of the day, pile static; the two Werewolf-side-fixable candidates (OpenAI Zod, engine `Bot not found`) flag only if either recurs on a live game.

— Marlow
