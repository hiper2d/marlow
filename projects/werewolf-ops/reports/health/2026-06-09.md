# Werewolf health — 2026-06-09

## Scan 02:46Z

Same 5-game errored pile (live ≤30d set), **no new errors** since the 20:03Z -08 scan. Empty digest, no issues, no urgent — the standing pile, all previously baselined. Ages advanced ~6.7h in step.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 217.2h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 443.6h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 546.2h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 637.0h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

The engine `Bot {"Cato":5} not found` game is now 637.0h (~26.5d), ~3.5 days from the 30-day TTL that retires it from the live set — still on track to age out before any code touches it. Two Werewolf-side code-fixable signatures unchanged (OpenAI Zod `.optional()`-without-`.nullable()`, engine `Bot not found`); neither on a live game. Ninth-plus static day — nothing actionable.

— Marlow

## Scan 07:41Z

Same 5-game errored pile, **no new errors** since the 02:46Z scan. Empty digest, no issues, no urgent — the standing baselined pile. Ages advanced ~4.9h in step.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 222.1h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 448.5h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 551.1h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 641.9h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

The engine `Bot {"Cato":5} not found` game is now 641.9h (~26.7d), ~3.3 days from the 30-day TTL — still on track to age out before any code touches it. Two Werewolf-side code-fixable signatures unchanged (OpenAI Zod `.optional()`-without-`.nullable()`, engine `Bot not found`); neither on a live game. Tenth-plus static day — nothing actionable.

— Marlow

## Scan 16:26Z

Same 5-game errored pile, **no new errors** since the 07:41Z scan. Empty digest, no issues, no urgent — the standing baselined pile. Ages advanced ~8.7h in step.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 230.8h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 457.3h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 559.9h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 650.6h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

The engine `Bot {"Cato":5} not found` game is now 650.6h (~27.1d), ~2.9 days from the 30-day TTL — still on track to age out before any code touches it. Two Werewolf-side code-fixable signatures unchanged (OpenAI Zod `.optional()`-without-`.nullable()`, engine `Bot not found`); neither on a live game. Eleventh-plus static day — nothing actionable.

— Marlow

## Scan 19:52Z

Same 5-game errored pile, **no new errors** since the 16:26Z scan. Empty digest, no issues, no urgent — the standing baselined pile. Ages advanced ~3.5h in step.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 234.3h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 460.7h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 563.3h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 654.1h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

The engine `Bot {"Cato":5} not found` game is now 654.1h (~27.3d), ~2.7 days from the 30-day TTL — still on track to age out before any code touches it. Two Werewolf-side code-fixable signatures unchanged (OpenAI Zod `.optional()`-without-`.nullable()`, engine `Bot not found`); neither on a live game. Twelfth-plus static day — nothing actionable.

— Marlow
