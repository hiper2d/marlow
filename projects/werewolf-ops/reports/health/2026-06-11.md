# Werewolf health — 2026-06-11

## Scan 03:00Z

Same 5-game errored pile, **no new errors** since the 19:59Z -10 scan. Empty digest, no issues, no urgent — the standing baselined pile. Ages advanced ~7h in step.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 265.4h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 491.8h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 594.4h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 685.2h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

The engine `Bot {"Cato":5} not found` game is now 685.2h (~28.5d), ~1.4 days from the 30-day TTL — still on track to age out before any code touches it. Two Werewolf-side code-fixable signatures unchanged (OpenAI Zod `.optional()`-without-`.nullable()`, engine `Bot not found`); neither on a live game. Nothing actionable.

— Marlow

## Scan 07:27Z

Identical pile, **no new errors** since the 03:00Z scan. Empty digest, `issues: []`, no urgent. Ages advanced ~4.5h in step.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 269.9h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 496.3h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 598.9h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 689.6h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

The engine `Bot not found` game is now 689.6h (~28.7d), ~1.3 days from the 30-day TTL — still set to age out on its own. Nothing actionable.

— Marlow

## Scan 16:27Z

Identical pile, **no new errors** since the 07:27Z scan. Empty digest, `issues: []`, no urgent. Ages advanced ~9h in step.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 278.9h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 505.3h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 607.9h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 698.6h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

The engine `Bot not found` game is now 698.6h (~29.1d), under a day from the 30-day TTL — next scan or two should see it age out. Worth confirming on tomorrow's first scan that the pile actually drops to 4; if it doesn't, the TTL assumption is wrong and that's a real (if minor) finding. Nothing else actionable.

— Marlow

## Scan 20:58Z

Identical pile, **no new errors** since the 16:27Z scan. Empty digest, `issues: []`, no urgent. Ages advanced ~4.5h in step.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 283.4h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 509.8h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 612.4h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 703.2h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

The engine `Bot not found` game is now 703.2h (~29.3d), ~0.7 days from the 30-day TTL — it crossed 29 days without dropping off, pile still 5 not 4. The "next scan or two should age it out" call from 16:27Z hasn't fired yet. If it's still here on tomorrow's first scan (past the 30d mark), the TTL-aging assumption is wrong and that's the real finding to chase. Nothing else actionable.

— Marlow
