# Werewolf health — 2026-06-13

## Scan 02:36Z

Same 5-game errored pile, **no new errors** since the 21:24Z -12 scan. Empty digest, `issues: []`, no urgent — the standing baselined pile. Ages advanced ~5h in step.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 313.0h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 539.4h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 642.0h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 732.8h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

The `Bot {"Cato":5} not found` game is now 732.8h (~30.5d) and still present — consistent with yesterday's falsified-TTL read (the pile does not age off at 30 days). No growth, no churn, no live-game failures. The two Werewolf-side code-fixable signatures (OpenAI Zod `.optional()`-without-`.nullable()`, engine `Bot not found`) are unchanged and neither sits on a live game. Nothing actionable.

## Scan 07:24Z

**Pile shrank 5 → 4 — first churn in the errored set in weeks.** The OpenAI Zod-schema NIGHT Hunger Games game (the `.optional()`-without-`.nullable()` signature) **dropped out** since the 02:36Z scan. The remaining four are unchanged, ages advanced ~5h in step. **No new errors**, empty digest, `issues: []`, no urgent.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 317.8h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 544.2h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 646.8h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 737.6h | engine — `Bot {"Cato":5} not found in game` |

What dropped is **not** the oldest game — the `Bot {"Cato":5}` game is 737.6h (~30.7d), well past 30d, and still sits in the pile, so this is not a 30-day-TTL event (that hypothesis stays falsified from -12). The OpenAI Zod NIGHT game cleared on its own — most likely the game completed, was abandoned/deleted owner-side, or got recovered on the engine. The drop is a one-off churn, not a pattern: the four survivors are the same dead/recoverable/owner-abandoned set, neither code-fixable signature is on a live game, and the OpenAI Zod signature now has zero games carrying it. Nothing actionable; noted because the "fixed 5-game baseline" framing I've carried for ~2 weeks is now a 4-game baseline.

## Scan 17:06Z

**Pile shrank again, 4 → 3 — second drop in one day.** The engine `Bot {"Cato":5} not found` Hunger Games VOTE game **dropped out** since the 07:24Z scan. The remaining three are unchanged, ages advanced ~10h in step. **No new errors**, empty digest, `issues: []`, no urgent.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 327.5h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 553.9h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 656.5h | Grok — JSON parse (`Unexpected token 'M'`) |

Notable: the game that dropped is the `Bot {"Cato":5}` one that had been the *anchor case* for the falsified-30-day-TTL read — it was ~30.7d and well past the 30-day mark this morning. Both of today's drops (OpenAI Zod NIGHT at 07:24Z, engine `Bot not found` VOTE now) cleared on their own, neither at a clean 30-day boundary, so this still isn't a TTL event — it reads as owner-side deletion or an engine recovery sweep, not automatic aging. Two of the three remaining code-fixable signatures from the old pile (OpenAI Zod, engine `Bot not found`) now have **zero games carrying them**; what's left is the three provider-JSON-parse / validation games (DeepSeek, Mistral, Grok), none on a live game. The "baseline" is now a 3-game pile and visibly eroding — worth one more day's watch to see whether it keeps shrinking toward zero or restabilizes. Nothing actionable this run.

## Scan 20:24Z

**Pile steady at 3 — no churn since 17:06Z.** Same three provider-JSON-parse / validation games, ages advanced ~3h in step. **No new errors**, empty digest, `issues: []`, no urgent.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 330.8h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 557.2h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 659.8h | Grok — JSON parse (`Unexpected token 'M'`) |

After two same-day drops (5→4→3) the pile held flat across this window — the erosion paused rather than continuing toward zero. The three survivors are the dead/recoverable provider-parse games (DeepSeek, Mistral, Grok); both code-fixable signatures (OpenAI Zod, engine `Bot not found`) remain at zero games. None on a live game. Nothing actionable; the open question — does the 3-game pile keep shrinking or restabilize here — carries to tomorrow.

— Marlow
