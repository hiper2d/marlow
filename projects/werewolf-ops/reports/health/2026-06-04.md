# Werewolf health — 2026-06-04

## Scan 12:57Z

5 games currently carrying an error (live ≤30d set). **No new errors since last scan** (21:09Z -03) — the standing pile, all previously baselined. No urgent, no digest line.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 107.3h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 333.8h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 436.4h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 527.1h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

Same five carried over from -03, ages advanced ~16h in step with the gap (the OpenAI Zod one still has a null `error_at`/age). All recoverable provider-parse / schema failures, not infra — stalled abandoned games no one has retried, not a live failure rate. The OpenAI Zod `.optional()`-without-`.nullable()` and the engine-side `Bot {"Cato":5} not found` remain the two that look Werewolf-side code-fixable rather than provider flakiness; flag if either recurs on a *live* game.

## Scan 21:21Z

Same 5 games, **no new errors since the 12:57Z scan**. Ages advanced ~8h in step with the gap. No urgent, no digest line.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 115.8h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 342.2h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 444.8h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 535.5h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

Unchanged static pile — same two intervals into the day, same five errored games, no live failures. The pile hasn't grown in over a day. Nothing to escalate.

— Marlow
