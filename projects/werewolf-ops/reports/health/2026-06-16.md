# Werewolf health — 2026-06-16

## Scan 02:56Z

**Pile still 3 — flat since the -15 22:54Z scan (~4h).** Same three provider-JSON-parse / validation games, ages advanced ~4h in step. **No new errors**, empty digest, `issues: []`, no urgent.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 385.3h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 611.8h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 714.4h | Grok — JSON parse (`Unexpected token 'M'`) |

Stable baseline holds: the pile has been exactly 3 (DeepSeek/Mistral/Grok provider-parse) across the entire -14→-16 window; both code-fixable signatures (OpenAI Zod, engine `Bot not found`) remain at zero games. None on a live game; all recoverable.

**The Grok game is now 714.4h ≈ 29.77d — still ~5.6h short of the 30-day mark (720h).** This first -16 scan just misses the threshold; the cleanest falsified-TTL test lands at the **09:00Z scan**, which puts the Grok game at ~720.5h, just past 30d. The verdict: if the Firestore 30-day TTL self-cleans, the Grok game vanishes at that scan or the next; if it persists past 720h, the read holds and these dead games clear only on owner-deletion or an engine purge. The Mistral game trails at 611.8h (~25.5d); Star Wars/DeepSeek at 385.3h (~16.1d). Nothing actionable this run — the next scan is the one to watch.

## Scan 07:24Z

**Pile still 3 — flat since the 02:56Z scan (~4.5h).** Same three provider-JSON-parse / validation games, ages advanced ~4.5h in step. **No new errors**, empty digest, `issues: []`, no urgent.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 389.8h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 616.2h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 718.8h | Grok — JSON parse (`Unexpected token 'M'`) |

Baseline still exactly 3 (DeepSeek/Mistral/Grok provider-parse) across the entire -14→-16 window; both code-fixable signatures (OpenAI Zod, engine `Bot not found`) remain at zero. None on a live game; all recoverable.

**The Grok game is now 718.8h ≈ 29.95d — still ~1.2h short of the 30-day mark (720h).** It crosses 30d at ~08:35Z; the next scan (09:00Z) puts it at ~720.4h, just past the threshold. That scan is the falsified-TTL verdict: if the Firestore 30-day TTL self-cleans, the Grok game drops off the live ≤30d set then or shortly after (TTL works); if it persists past 720h, the prior read holds — dead games clear only on owner-deletion or an engine purge. Mistral trails at 616.2h (~25.7d); Star Wars/DeepSeek at 389.8h (~16.2d). Nothing actionable this run.

## Scan 16:26Z

**Pile dropped 3 → 2 — the Grok game crossed 30d and self-cleaned.** No new errors (`issues: []`, empty digest, no urgent); the only change since 07:24Z is the disappearance of the `werewolf` / Grok game, which was 718.8h (~29.95d) and present at the 07:24Z scan and is gone now that it's past 720h.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 398.8h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 625.3h | Mistral — response validation failed |

**30-day TTL verdict: self-clean CONFIRMED for the Grok game.** It vanished from the errored set right after crossing 720h, exactly as a working Firestore `expireAt` TTL predicts. This *complicates* the earlier -12 read where an engine `Bot not found` game appeared to persist past 30d (the "falsified-TTL" call): the two games behave differently, so the cleanest current statement is that the TTL does fire — the -12 case was likely a lag or a different `expireAt` provenance, not a broken TTL. Net: dead errored games do age off near 30d on their own; no owner-deletion or engine purge was needed here. The remaining two (Mistral ~26.1d, DeepSeek ~16.6d) should follow the same path — Mistral next, in ~4 days. Both code-fixable signatures (OpenAI Zod, engine `Bot not found`) remain at zero. Nothing actionable this run.

## Scan 18:41Z

**Pile still 2 — flat since the 16:26Z scan (~2h).** Same two provider-parse/validation games, ages advanced ~2h in step. **No new errors** (`issues: []`, empty digest, no urgent).

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 401.1h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 627.5h | Mistral — response validation failed |

Mistral now ~26.1d, Star Wars/DeepSeek ~16.7d — both still inside the 30d TTL window, tracking toward self-clean (Mistral next, ~4 days out). Both code-fixable signatures (OpenAI Zod, engine `Bot not found`) remain at zero. Nothing actionable this run.

— Marlow
