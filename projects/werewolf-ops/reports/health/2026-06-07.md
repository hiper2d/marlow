# Werewolf health — 2026-06-07

## Scan 13:22Z

5 games currently carrying an error (live ≤30d set). **No new errors since last scan** (19:43Z -06) — the standing pile, all previously baselined. No urgent, no digest line.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 179.8h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 406.2h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 508.8h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 599.6h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

Same five carried over from -06; ages advanced ~17.7h in step with the overnight gap (the OpenAI Zod entry still has a null `error_at`/age). All recoverable provider-parse / schema failures on stalled abandoned games — not a live failure rate. The engine `Bot {"Cato":5} not found` game is now at 599.6h (~25.0d), ~5.0 days from the 30-day TTL that retires it from the live set. The OpenAI Zod `.optional()`-without-`.nullable()` and that engine `Bot not found` remain the two that look Werewolf-side code-fixable rather than provider flakiness; flag only if either recurs on a *live* game. Pile static into a fifth-plus day; no live-game involvement.

## Scan 00:42Z (-08 UTC; local -07 evening)

Same 5-game pile, **no new errors** since 13:22Z. No issues, no urgent, no digest line.

| Theme | State | Recoverable | Age | Provider / failure |
|---|---|---|---|---|
| Star Wars | WELCOME | yes | 191.1h | DeepSeek — JSON parse (`Expected ',' or '}'`) |
| Hunger Games | WELCOME | yes | 417.5h | Mistral — response validation failed |
| werewolf | DAY_DISCUSSION | yes | 520.1h | Grok — JSON parse (`Unexpected token 'M'`) |
| Hunger Games | VOTE | yes | 610.9h | engine — `Bot {"Cato":5} not found in game` |
| Hunger Games | NIGHT | yes | ? | OpenAI — Zod schema (`.optional()` without `.nullable()`) |

Ages advanced ~11.3h since the morning scan, in step. The engine `Bot {"Cato":5} not found` game is now 610.9h (~25.5d), ~4.5 days from the 30-day TTL. Still the same two Werewolf-side-fixable signatures (OpenAI Zod, engine `Bot not found`); neither has touched a live game. Sixth-plus static day — nothing actionable.

— Marlow
