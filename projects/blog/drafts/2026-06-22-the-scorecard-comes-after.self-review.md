---
slug: 2026-06-22-the-scorecard-comes-after
reviewed_at: 2026-06-22T16:42:13Z
verdict: hold-for-alex
pauses_triggered: [header-image-embedded-text]
---

## Per-rubric notes

**Voice:** Strong and clean. Plain, fact-first, concrete early hook (92% vs 54% on GPT-5.4 in the second paragraph, not buried). Job-named section headers carry the thesis. Two exclusion-list near-misses I'd let live under the journal's standing test — "The setups aren't adversarial; they're the ordinary work the model is deployed to do" (mirrored contrast, but carries the real claim) and "No jailbreak, no trap, and the behavior still surfaces" (negation-ish, but it's the actual finding). Ends one sentence on the hard concrete beat — "the one with no stage lights to turn off" — a callback image, not a verdict closer. `— Marlow` signature present.

**Structure:** 4 citations, correctly sectioned by *what each source proves* ("What it buys," "The detector that fails on the real case," "Where the misbehavior comes from") rather than by lab — the lit-dense default rule held. ~900 words, inside the ceiling. Citation hygiene good: primary sources throughout (arXiv for ROGUE, AF for the OpenAI method), each cited once. Lands on synthesis, no watch-list reflex.

**Topic:** Through-line nameable in one sentence — deployment-realism grading is the first move on this arc that *closes* the eval-awareness gap, but it forecasts an aggregate rate after the model ships and can't catch the instance that matters. This is post #3 on cot-monitorability, and it deliberately breaks the arc's 5/5 DeepMind-interp streak: anchors are OpenAI, a LessWrong proposal, and CMU's ROGUE. The draft names the move in the body ("it comes from outside the interpretability labs that supplied most of the last post"), so pause 7 is consciously resolved, not tripped — the streak is broken by source, exactly what the arc needed.

**Pre-publish pauses:** Pause 6 (header image) triggers. The image — an antique brass rain-gauge on a weathered windowsill, muted earth tones, lithograph grain, symbolic-not-literal (a gauge that reads the weather *after* it arrives = "the scorecard comes after") — is otherwise on-brand and clears every other visual red flag. But it embeds legible numerals: "MM" and the scale "50 40 30 20 10" down the side. That is embedded text inside the image, a named pause-6 trigger, and it matches the 2026-06-04 precedent exactly (the unbundling-the-intelligence-explosion header embedded ruler numerals and was regenerated text-free in-tick). Pauses 1-5 clear; pause 7 does not trigger (sources are diverse).

## Verdict rationale

Prose and structure are ship-quality, and the source diversity is precisely the fix this arc has been waiting three posts for. The hold is entirely on the header: it embeds legible scale numerals, which trips pause 6, and the blog's own standing precedent is to regenerate such headers text-free rather than ship them. The self_review stage doesn't own image regeneration, so I can't fix it here — the disciplined call per the rubric ("if the image triggers a red flag, hold regardless of prose quality") is hold-for-alex. The remediation is narrow and known: regenerate the same rain-gauge concept with the scale rendered as bare tick marks (no numerals, no "MM"), then this is a clean ship. Surfacing to Alex so he can release-with-regen rather than letting a strong piece publish with an embedded-text header.
